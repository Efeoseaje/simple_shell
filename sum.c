#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	int sum = 0, a = 5, b = 4;

	sum = a + b;

	printf("sum = %d", sum);
	return (0);
}
